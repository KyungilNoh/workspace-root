# 네이버 지도 (Location, ContactSection) — 클라이언트에서 사용
NEXT_PUBLIC_NAVER_MAP_CLIENT_ID=your_naver_map_client_id

# Contact API (Gmail)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_USER=
TO_EMAIL=

# iframe 모드 (app-shell에서 iframe으로 로드할 때만 필요)
# 독립 실행 시에는 설정하지 않거나 false로 설정
# NEXT_PUBLIC_USE_IFRAME=true
